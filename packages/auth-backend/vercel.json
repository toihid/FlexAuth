{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install && pnpm run build --filter @flexauth/shared-types",
  "builds": [
    {
      "src": "src/vercel.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/vercel.ts"
    }
  ]
}
